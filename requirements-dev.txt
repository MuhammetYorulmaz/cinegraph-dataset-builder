# Optional dependencies used by the analysis notebook and test suite.
# The core pipeline (scripts/run_pipeline.py) does not require these.
jupyter>=1.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
pytest>=7.0.0
